# ===================================================================
# E2E BASE CONFIGURATION (COMMON TO ALL TEST ENVIRONMENTS)
# ===================================================================
e2e.env=local
e2e.http.timeout.connect-ms=5000
e2e.http.timeout.read-ms=10000

# Logging (Plain-text Console, No JSON)
logging.structured.format.console=
